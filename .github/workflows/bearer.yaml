name: Bearer

on:
  pull_request: 
  push:
    branches:
      - master

permissions:
  contents: read

jobs:
  rule_check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Bearer
        uses: bearer/bearer-action@v2
        # Uncomment to get results for critical and high vulnerabilies only
        # with:
        #   severity: "critical,high"
